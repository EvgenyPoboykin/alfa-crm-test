import styled from 'styled-components';
import {} from './interfaces';

export const Container = styled.div.attrs({ className: 'HeaderDescription__container' })`
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
`;
