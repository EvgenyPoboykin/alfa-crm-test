import { IFormObject } from './interfaces';

export const formObject: IFormObject = {
    email: '',
    password: '',
    rememberMe: false,
    fetching: false,
    token: '',
    redirect: true,
};
